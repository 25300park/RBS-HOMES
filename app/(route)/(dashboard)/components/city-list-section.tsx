import CityGrid from "./city-grid";

export interface ReginListSectionProps {
  
};



const ReginListSection = ({  }: ReginListSectionProps): React.ReactNode => {
  return <section>
    <CityGrid />
  </section>
};

export default ReginListSection;


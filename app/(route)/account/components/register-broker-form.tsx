export interface RegisterBrokerFormProps {
  session: any
};

const RegisterBrokerForm = ({ session }: RegisterBrokerFormProps): React.ReactNode => {
  return <div>11</div>
};

export default RegisterBrokerForm;